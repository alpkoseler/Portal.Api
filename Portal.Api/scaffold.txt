Scaffold-DbContext "Host=localhost;Port=5432;Database=portaldb;Username=postgres;Password=17410082013Aa;Trust Server Certificate=true;" Npgsql.EntityFrameworkCore.PostgreSQL -OutputDir Models -Context AppDbContext -f

!_______________ unutma
    protected override void OnConfiguring(DbContextOptionsBuilder optionsBuilder)
    {
        if (!optionsBuilder.IsConfigured)
        {
            var configuration = new ConfigurationBuilder()
                .SetBasePath(AppDomain.CurrentDomain.BaseDirectory)
                .AddJsonFile("appsettings.json")
                .Build();

            optionsBuilder.UseNpgsql(configuration.GetConnectionString("DefaultConnection"));
        }
    }